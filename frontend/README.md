# Axiom Frontend

The Next.js frontend for Axiom, a system for evaluating general knowledge,
technology, science, and AI-generated claims against reliable sources.

## Local development

Start the FastAPI backend on port 8000, then run:

```bash
npm install
npm run dev
